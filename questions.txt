1) Какой вид роутинга предпочтительней

angular.module('todo_list', ['ngResource']).factory('Project', function($resource){
    return {
        list:  $resource('/projects/list', {}, {
            query: {
                method: 'POST',
                isArray: false
            }
        }),
        menuList:  $resource('/projects/menu/list', {}, {
            query: {
                method: 'POST',
                isArray: false
            }
        }),
        project:  $resource('/projects/project', {}, {  // load project page
            query: {
                method: 'POST',
                isArray: false
            }
        }),
        project_tasks:  $resource('/projects/project_tasks', {}, {  // load tab tasks of project page
            query: {
                method: 'POST',
                isArray: false
            }
        })
    });



2) Запуск всех тестов валит feature тесты, по отдельности все нормально
    ошибка: 'ActiveRecord::StatementInvalid:
                     SQLite3::BusyException: database is locked:'
                     timeout выставлял более 10 секунд не помогло


3) Какой инструмент использовать для тестирования js
4) шаблоны для angular через asset_path или angular-rails-template
5) правильно ли решение по использованию cancan